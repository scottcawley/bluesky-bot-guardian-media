name: RSS to Bluesky Pro (Final Cleaned)
on:
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes
  workflow_dispatch:

jobs:
  post-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6 # Updated to v6 to clear Node warnings

      - name: Set up Python
        uses: actions/setup-python@v6 # Updated to v6 to clear Node warnings
        with:
          python-version: '3.13' # Using latest 2026 Python stable

      - name: Install dependencies
        run: pip install atproto feedparser beautifulsoup4 requests lxml

      - name: Run Bot
        env:
          BSKY_HANDLE: ${{ secrets.BSKY_HANDLE }}
          BSKY_PASSWORD: ${{ secrets.BSKY_PASSWORD }}
          RSS_URL: 'https://www.theguardian.com/uk/media/rss'
        run: |
          python - <<EOF
          import os, feedparser, requests, re
          from atproto import Client, models, client_utils
          from bs4 import BeautifulSoup

          client = Client()
          client.login(os.environ['BSKY_HANDLE'], os.environ['BSKY_PASSWORD'])
          
          # 1. Deduplication: Peek at last post
          profile_feed = client.get_author_feed(actor=os.environ['BSKY_HANDLE'], limit=1)
          last_link_on_bsky = ""
          if profile_feed.feed:
              last_post = profile_feed.feed[0].post
              if last_post.embed and hasattr(last_post.embed, 'external'):
                  last_link_on_bsky = last_post.embed.external.uri

          # 2. Parse RSS
          feed = feedparser.parse(os.environ['RSS_URL'])
          if not feed.entries: exit()
          entry = feed.entries[0]

          if entry.link == last_link_on_bsky:
              print("No new content found. Stand down.")
          else:
              # 3. Targeted Extraction (Captures only the first <p> tag text)
              raw_desc = entry.get('summary', entry.get('description', ''))
              soup = BeautifulSoup(raw_desc, 'html.parser')
              
              # Find the first paragraph
              first_p = soup.find('p')
              if first_p:
                  clean_desc = first_p.get_text(strip=True)
              else:
                  # Robust fallback
                  clean_desc = soup.get_text(separator=' ').strip().split('\n')[0][:200]
              
              # Scrub extra whitespace
              clean_desc = re.sub(r'\s+', ' ', clean_desc)

              # 4. Scrape Image & Create Link Card
              embed = None
              try:
                  page = requests.get(entry.link, timeout=10)
                  soup_page = BeautifulSoup(page.content, 'html.parser')
                  og_image = soup_page.find("meta", property="og:image")
                  if og_image:
                      img_data = requests.get(og_image["content"]).content
                      upload = client.upload_blob(img_data)
                      embed = models.AppBskyEmbedExternal.Main(
                          external=models.AppBskyEmbedExternal.External(
                              title=entry.title,
                              description=clean_desc,
                              uri=entry.link,
                              thumb=upload.blob
                          )
                      )
              except Exception as e:
                  print(f"Image scrape failed: {e}")

              # 5. Post Construction with shorter link text
              tb = client_utils.TextBuilder()
              tb.text(f"{entry.title}\n\n")
              tb.link("More on The Guardian", entry.link)

              client.send_post(tb, embed=embed)
              print(f"Success: Posted '{entry.title}'")
          EOF
